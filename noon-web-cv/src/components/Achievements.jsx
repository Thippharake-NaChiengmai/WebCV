import React, { useState } from 'react';
import { awards } from '../data/awards';

const Achievements = () => {
  const [selectedImages, setSelectedImages] = useState({});
  return <section className="card shadow-lg border-0 mb-4"><div className="card-header bg-primary text-white py-3"><h2 className="h4 mb-0 fw-bold"><i className="bi bi-trophy me-2" />Awards</h2></div><div className="card-body p-4">{awards.map((award, awardIndex) => { const selectedIndex = selectedImages[awardIndex] || 0; const selectedImage = award.images[selectedIndex]; return <article key={award.title} className="mb-4 last-child-mb-0"><h3 className="h5 fw-bold text-primary mb-1">{award.title}</h3><p className="text-muted small">{award.date}</p><ul>{award.details.map(detail => <li key={detail}>{detail}</li>)}</ul>{selectedImage ? <div className="mt-3"><img src={selectedImage.src} alt={selectedImage.alt} className="img-fluid rounded" style={{ maxHeight: 360, objectFit: 'cover' }} />{selectedImage.caption && <p className="small text-muted mt-2 mb-0">{selectedImage.caption}</p>}{award.images.length > 1 && <div className="d-flex flex-wrap gap-2 mt-3">{award.images.map((image, imageIndex) => <button type="button" key={image.src} onClick={() => setSelectedImages({ ...selectedImages, [awardIndex]: imageIndex })} className={`border rounded p-0 ${selectedIndex === imageIndex ? 'border-primary border-3' : 'border-light'}`} aria-label={`Show award image ${imageIndex + 1}`}><img src={image.src} alt="" style={{ width: 72, height: 54, objectFit: 'cover' }} /></button>)}</div>}</div> : <div className="border rounded bg-light text-muted d-flex align-items-center justify-content-center mt-3" style={{ minHeight: 120 }}><span><i className="bi bi-images me-2" />Award images coming soon</span></div>}</article>; })}</div></section>;
};
export default Achievements;
